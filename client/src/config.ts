export const loginMethod = 'fake'; // remove this after project completed
export const settings = {
    theme: "light",
}
export const permissions = {
    admin: 'all',
}
export const MAX_FILE_SIZE = '128MB';